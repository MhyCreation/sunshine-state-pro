node_modules
.next
out
.env
.env.local
.env*.local
.DS_Store
*.log
.vercel

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Localbase — runtime data (SQLite file, storage uploads)
.localbase/
